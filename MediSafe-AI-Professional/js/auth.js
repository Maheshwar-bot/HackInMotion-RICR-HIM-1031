document.querySelectorAll('[data-toggle-password]').forEach(btn=>btn.addEventListener('click',()=>{const input=document.querySelector(btn.dataset.togglePassword);input.type=input.type==='password'?'text':'password';btn.textContent=input.type==='password'?'◉':'◎'}));
function authMessage(text,type='ok'){const el=document.getElementById('authMessage');if(el){el.textContent=text;el.className='message '+type}}
const loginForm=document.getElementById('loginForm');
if(loginForm) loginForm.addEventListener('submit',e=>{e.preventDefault();const email=document.getElementById('loginEmail').value.trim(),pass=document.getElementById('loginPassword').value;if(!email||!pass)return authMessage('Please enter your email/phone and password.','error');const remembered=document.getElementById('remember').checked;const user=JSON.parse(localStorage.getItem('medisafe_profile')||'null')||{name:email.split('@')[0],email};localStorage.setItem('medisafe_profile',JSON.stringify(user));if(remembered)localStorage.setItem('medisafe_logged','1');else sessionStorage.setItem('medisafe_logged','1');authMessage('Login successful. Opening your dashboard…');setTimeout(()=>location.href='home.html',350)});
const registerForm=document.getElementById('registerForm');
if(registerForm) registerForm.addEventListener('submit',e=>{e.preventDefault();const name=document.getElementById('regName').value.trim(),email=document.getElementById('regEmail').value.trim(),p=document.getElementById('regPassword').value,c=document.getElementById('regConfirm').value;if(!name||!email||!p||!c)return authMessage('Please complete all fields.','error');if(p.length<6)return authMessage('Password should contain at least 6 characters.','error');if(p!==c)return authMessage('Passwords do not match.','error');if(!document.getElementById('terms').checked)return authMessage('Please accept the terms to continue.','error');localStorage.setItem('medisafe_profile',JSON.stringify({name,email}));localStorage.setItem('medisafe_logged','1');authMessage('Account created. Opening your dashboard…');setTimeout(()=>location.href='home.html',450)});
const forgotForm=document.getElementById('forgotForm');
if(forgotForm) forgotForm.addEventListener('submit',e=>{e.preventDefault();const email=document.getElementById('forgotEmail').value.trim();if(!email)return authMessage('Enter your email address.','error');authMessage('Demo reset request created. Connect Supabase Auth for real email delivery.');});
document.querySelectorAll('[data-demo-social]').forEach(b=>b.addEventListener('click',()=>authMessage(`${b.dataset.demoSocial} sign-in is a UI demo. Connect OAuth to make it production-ready.`)));